/**
 * Centralized exports for all custom hooks
 */

// Rick and Morty API hooks
export {
  useEpisode,
  useLocation,
  useEpisodes,
  useCharacter,
  useLocations,
  useCharacters,
  useCharactersByIds,
} from './useRickAndMorty'

// Other hooks
export { useTheme } from './useTheme'
export { useLocale } from './useLocale'
